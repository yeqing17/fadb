# 更新日志

本文件记录 fadb 桌面应用每个版本的可见变化,格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/),版本号遵循语义化版本。每个版本对应仓库里的 `v*` 标签;更早的零散发布(BridgeScope 时期 0.4.x—0.6.x)不再逐条回填,可在 [Releases 页面](https://github.com/yeqing17/fadb/releases)按标签查看。

## [0.8.10] - 2026-09-06

### 新增

- 应用内下载安装更新:发现新版本后设置窗口可直接「下载并安装」,显示进度条并支持取消;下载完成先对照发布页 SHA256SUMS 清单做 SHA-256 校验,点「重启并完成更新」即自动换用新版本并重启,旧版本备份在下次启动时静默清理;下载或安装失败(杀毒拦截、目录需要管理员权限等)给出本地化提示与手动下载入口。
- 发布资产新增裸可执行文件(fadb-desktop-<tag>-x86_64-pc-windows-msvc.exe)及覆盖它的 SHA256SUMS 条目,更新下载不再解析 zip;发布页没有当前平台的可执行文件时(非 Windows 构建或旧版本发布页),更新入口自动降级为「查看发布页」跳转。

## [0.8.9] - 2026-09-06

### 新增

- 更新检查:设置窗口「关于」区新增「检查更新」按钮与状态显示(检查中/已是最新/发现新版本/失败原因),发现新版本可一键打开发布页;「启动时自动检查更新」默认开启,启动数秒后静默检查一次,24 小时内不重复,失败的检查不计入间隔、下次启动自动重试。
- 启动检查发现新版本时,底部弹出加长的「发现新版本 vX.Y.Z」提示,不依赖设置窗口是否打开。
- 更新检查自动走系统代理:HTTP 客户端启用 system-proxy,读取 Windows/macOS 的系统代理设置,代理环境无需额外配置环境变量;失败提示附本地化建议。

## [0.8.8] - 2026-09-03

### 新增

- 应用面板支持把 APK 直接拖进窗口安装:悬停在窗口任意位置即显示全窗高亮与「释放以安装 APK」及文件名,松手后自动切到应用面板开始安装;未选择设备时给出提示,已有安装进行中时忽略新投放。

### 修复

- APK 安装失败且 adb 未返回原因时(小米/MIUI 电视上安装确认被拒或超时、连接中断等场景常见),错误横幅不再只剩一个悬空的冒号,改为附上本地化的可操作提示(到设备屏幕确认安装、检查连接后重试);stdout 的 `Failure []` 同样按无原因处理。

## [0.8.7] - 2026-09-03

### 新增

- 概览面板点击即复制:任意字段整格可点,悬停显示复制光标与「点击复制」提示,复制成功后内容区底部弹出淡出的「已复制」提示。
- 进程面板:支持关键字搜索(进程名、PID、用户,大小写不敏感);点击 CPU / 内存 / 常驻内存 列头按该列降序排序,再点一次恢复默认,缺失数据始终沉底。
- 性能面板:图表改用 egui_plot 绘制——渐变面积填充、带刻度坐标轴、悬浮十字准星读数;新增「停止/开始」按钮控制持续采样,「刷新」保留单次采样。

### 变更

- 性能采集优化:每个采样点由 5 次 adb 往返合并为 1 条 shell 调用,CPU 占用改由两次 /proc/stat 差分计算,不再调用缓慢的 dumpsys cpuinfo;轮询间隔降至 500ms 并按设备实际速度自适应。

### 修复

- 性能图表从右缘起画、随时间向左流动,不再把少量样本拉伸铺满整个窗口;样本按真实时间戳定位,时间轴流速与实际一致。
- 性能图表 y 轴中间刻度(如 1000–6000 MB)不再因刻度过密而几乎不可见,全部以正常文字亮度渲染。
- 自动采样期间「刷新」按钮不再明暗闪烁;进程面板的转圈与闪烁一并移除。
- 性能与进程面板标题行的提示文字与按钮基线对齐(对齐助手移入 theme 共用)。

## [0.8.6] - 2026-09-02

### 修复

- 顶栏品牌区与设备选择器对齐:字标改用拉丁专用字体族并按 logo 高度光学居中;品牌组下移使其下边沿与选择框对齐;设备选择框从占满整行高度改为紧凑居中,与相邻按钮同一基线。
- README 中英两份重写:功能按设备/面板/界面分组呈现,补充设置窗口、可折叠导航、终端复制粘贴等 0.8.x 能力,移除过时表述。

## [0.8.5] - 2026-09-02

### 新增

- 终端支持文本拖拽选中:按网格高亮,松开即复制到剪贴板;右键菜单提供复制所选、粘贴与复制可见内容;工具栏新增「粘贴」按钮,遵守服务端的括号粘贴模式(Ctrl+V 直通不变,Ctrl+C 仍发送中断信号)。
- 左侧导航栏可折叠:顶部「«/»」按钮在完整列表与 40 点图标栏之间切换,折叠状态本地持久化;展开宽度固定 125 点,不再可拖拽。

### 修复

- 文件管理器的列表不再水平收缩:此前滚动区域默认紧贴内容宽度,修改时间列被拦腰截断、滚动条卡在面板中间;现在列表始终撑满面板宽度。

## [0.8.4] - 2026-09-01

### 新增

- 设置窗口与 ADB 诊断窗口新增「官方下载」入口,一键打开 Android 平台工具(platform-tools)下载页;中文界面使用 Google 中国镜像域名,英文界面使用 developer.android.com。
- `FADB_ADB` 环境变量可直接指定 `adb` 可执行文件路径,优先级高于 `PATH`、`ANDROID_SDK_ROOT` 与 `ANDROID_HOME`;指向不存在的文件时会在界面明确报错,不再静默回退到自动查找。

### 修复

- 水平行内按钮文字与纯文本标签的基线不再错位(设置页 ADB 信息行、官方下载行等;此前按钮文字低 4 像素)。
- 中文字体改为主字体而非回退字体,「AI 助手」等中西文混排标签与相邻纯中文标签共享同一条基线,不再有 1 像素偏移。
- 顶栏设备选择器的左边缘与左侧导航面板边界对齐;左侧导航收窄至 150 点。
- 设置窗口「外观 / ADB / 关于」三个分组标题恢复正文字号,三个分组第二列起始位置一致。
- 中央面板的错误横幅按语言显示标题,不再直接输出内部键名;左侧导航项 WebView 中文化为「网页」。

## [0.8.3] - 2026-08-31

### 新增

- 网络设备一键断开:设备管理器为已连接的网络端点提供「断开」按钮。
- 设置窗口 ADB 信息的「详情 / 收起」浮动窗口:展示可执行文件路径、ADB 版本与检测到的设备数。

## [0.8.2] - 2026-08-31

### 新增

- 设置窗口:外观(浅色/深色主题)、界面语言(中文/English)、ADB 检测信息(可执行文件、版本、设备数)与关于区块,入口在顶栏齿轮按钮。

## [0.8.1] - 2026-08-31

### 新增

- fadb 品牌 logo:窗口图标、可执行文件图标与应用内标题栏标识。

### 修复

- AI 助手不再杜撰不存在的 `fadb` 命令行工具,系统提示词与文档同步修正。

## [0.8.0] - 2026-08-30

### 新增

- scrcpy 投屏遥控:按键行遥控设备,一键把当前画面录成 MP4。
- 终端快捷指令:可编辑的命令按钮栏,支持本地持久化与 JSON 导入/导出。

### 变更

- 项目由 BridgeScope 更名为 fadb。

## [0.7.1] - 2026-08-30

### 移除

- 移除 0.6.0 引入的 AVD 管理器面板。

## [0.7.0] - 2026-08-30

### 新增

- scrcpy 投屏:转发隧道显示设备画面(仅视频,可调最大尺寸/码率)。
